# Test app for using [grunt-node-webkit-builder](https://github.com/mllrsohn/grunt-node-webkit-builder)

## Usage

Install the dependencies:

    $ npm install

Then run the default grunt task:

    $ grunt

Inspect the `dis/builder-test` folder for output.
